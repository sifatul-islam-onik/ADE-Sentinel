# ADE-Sentinel — REMOTE environment (Kaggle 2xT4 primary, Colab fallback).
# Every neural run and the embedding training execute here.
#
# Kaggle and Colab base images already ship torch / transformers / datasets and
# UPDATE THEM WITHOUT WARNING. Reinstalling torch on Kaggle is slow and can break
# CUDA. So this file is split in two (PLAN F10):
#
#   GROUP A - pip install these yourself, pinned.
#   GROUP B - do NOT install; RECORD the preinstalled version in results/runs.csv
#             so a run stays reproducible even after the base image moves.
#
# First notebook cell (use sys.executable -m pip, NOT bare !pip - see below):
#   python -m pip install -q gensim==4.4.0 pytorch-crf==0.7.2
#   import torch, transformers, datasets, numpy
#   print(torch.__version__, transformers.__version__, datasets.__version__,
#         numpy.__version__, torch.cuda.device_count())
#
# Paste that printed line into the run row. It is the remote half of the
# reproducibility record; the git commit hash is the local half.

# ---------------- GROUP A: install, pinned ----------------
gensim==4.4.0
pytorch-crf==0.7.2

# ---------------- DELIBERATELY NOT INSTALLED REMOTELY ----------------
# seqeval==1.2.2 - LOCAL ONLY. 1.2.2 is its latest release (2020), it ships no
# wheel, and its legacy `setup.py egg_info` fails to build on Kaggle's Python
# 3.12 with `metadata-generation-failed`. Stage 2 therefore scores entities
# with the native implementation in src/stage2_metrics.py, which needs only
# the standard library. Locally, entity_metrics runs both and asserts they
# agree; they match exactly on strict and lenient F1, precision, recall and
# per-label scores over all 4,271 sentences.
#
# NOTE ON `!pip`: in a Kaggle notebook `!pip` resolves to whatever pip is
# first on PATH, which is not always the interpreter running the kernel. The
# install then reports success and the import still fails. Always install via
# `subprocess.run([sys.executable, '-m', 'pip', ...])` and check the exit code.

# ---------------- GROUP B: record, do not install ----------------
# torch                 (Kaggle preinstalled, CUDA-matched - never reinstall)
# transformers          (Kaggle preinstalled)
# datasets              (Kaggle preinstalled)
# numpy                 (pinned by the base image; gensim 4.4.0 supports numpy 2.x)
# scikit-learn          (Kaggle preinstalled)

# NOTE ON GENSIM + NUMPY: gensim < 4.3.3 is incompatible with numpy 2.x. If the
# base image ever pins numpy < 2 and gensim resolution fails, downgrade gensim
# rather than touching numpy - numpy is load-bearing for torch on that image.
